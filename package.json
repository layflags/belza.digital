{
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "deploy": "firebase deploy --only hosting --token \"$FIREBASE_TOKEN\""
  },
  "devDependencies": {
    "firebase-tools": "^14.19.1"
  }
}
