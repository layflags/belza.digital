{
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "deploy": "firebase deploy --only hosting --project belza-digital"
  },
  "devDependencies": {
    "firebase-tools": "^14.19.1"
  }
}
